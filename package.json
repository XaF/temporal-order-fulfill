{
  "name": "temporal-order-fulfill",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "tsc --build",
    "build.watch": "tsc --build --watch",
    "lint": "eslint .",
    "start": "ts-node src/worker.ts",
    "start.watch": "nodemon src/worker.ts",
    "workflow": "ts-node src/client.ts",
    "disable-inventory-service": "echo 0 > inventory-service-available",
    "enable-inventory-service": "rm -f inventory-service-available",
    "demo.0": "bash demo/demo0.sh",
    "scenario.0": "if [ -f \"src/workflows.ts.save\" ]; then mv src/workflows.ts.save src/workflows.ts; fi",
    "scenario.1": "if ! [ -f \"src/workflows.ts.save\" ]; then mv src/workflows.ts src/workflows.ts.save; fi; cp demo/workflows1.ts src/workflows.ts",
    "scenario.2": "if ! [ -f \"src/workflows.ts.save\" ]; then mv src/workflows.ts src/workflows.ts.save; fi; cp demo/workflows2.ts src/workflows.ts",
    "scenario.3": "if ! [ -f \"src/workflows.ts.save\" ]; then mv src/workflows.ts src/workflows.ts.save; fi; cp demo/workflows3.ts src/workflows.ts"
  },
  "nodemonConfig": {
    "execMap": {
      "ts": "ts-node"
    },
    "ext": "ts",
    "watch": [
      "src"
    ]
  },
  "dependencies": {
    "@temporalio/activity": "^1.10.2",
    "@temporalio/client": "^1.10.2",
    "@temporalio/worker": "^1.10.2",
    "@temporalio/workflow": "^1.10.2",
    "yargs": "^17.7.2"
  },
  "devDependencies": {
    "@tsconfig/node18": "^1.0.3",
    "@types/node": "^16.11.43",
    "@types/yargs": "^17.0.32",
    "@typescript-eslint/eslint-plugin": "^5.0.0",
    "@typescript-eslint/parser": "^5.0.0",
    "eslint": "^7.32.0",
    "eslint-config-prettier": "^8.3.0",
    "eslint-plugin-deprecation": "^1.2.1",
    "nodemon": "^2.0.12",
    "prettier": "^2.8.8",
    "ts-node": "^10.8.1",
    "typescript": "^4.4.2"
  }
}
