{
  "build": { "dockerfile": "Dockerfile" },
  "overrideCommand": false,
  //"postStartCommand": "WATCHDIR=$PWD; cd /tmp && nohup bash -c \"/watcher.sh \\\"${WATCHDIR}\\\" &>/tmp/logs/watcher.log &\"; rm nohup.out",
  "postCreateCommand": "npm install",
  "postStartCommand": "nohup bash -c \"npm run start.watch &>/tmp/logs/temporal-worker.log &\" && rm nohup.out",
  "forwardPorts": [
    7233,
    7243,
    8080,
    9090
  ],
  "portsAttributes": {
    "7233": {
      "label": "temporal-grpc",
      "onAutoForward": "silent"
    },
    "7243": {
      "label": "temporal-http",
      "onAutoForward": "silent"
    },
    "8080": {
      "label": "temporal-web-ui",
      "onAutoForward": "openBrowser"
    },
    "9090": {
      "label": "temporal-metrics",
      "onAutoForward": "silent"
    }
  },
  "customizations": {
    "vscode": {
      "extensions": [
        //"zucska.task-button"
      ]
    }
  }
}
